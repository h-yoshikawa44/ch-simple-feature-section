{
  "name": "ch-simple-feature-section",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint-check": "npm run lint:markuplint && npm run check:prettier",
    "lint:markuplint": "markuplint \"./**/*.html\"",
    "check:prettier": "prettier --check \"./**/*.{html,css,json,yml}\"",
    "fix": "npm run fix:markuplint && npm run fix:prettier",
    "fix:markuplint": "npm run lint:markuplint -- --fix",
    "fix:prettier": "npm run check:prettier -- --write"
  },
  "devDependencies": {
    "markuplint": "^4.12.0",
    "prettier": "^3.5.3",
    "vite": "^6.3.1"
  },
  "volta": {
    "node": "22.14.0"
  }
}
